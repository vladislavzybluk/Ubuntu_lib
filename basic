lsb_release -a    # Проверяем версию оси
pwd               # уточняем в какой папке мы находимся

ls                # чтобы узнать, какие файлы находятся в каталоге, можете увидеть все скрытые файлы, используя команду "ls -a".
cd                # используйте команду "cd", чтобы перейти в каталог

mkdir XX          # и rmdir когда вам нужно создать папку или каталоg c именем XX

rm                # используйте команду rm для удаления файлов и каталогов. Используйте «rm -r», 
                  # чтобы удалить только каталог. Он удаляет как папку, так и содержащиеся в ней файлы при использовании только команды rm.
touch             # touch - команда touch используется для создания файла.

man # и --help    # - Чтобы узнать больше о команде и о том, как ее использовать, используйте команду man. 
                  # Показывает справочные страницы команды. Например, «man ls» показывает справочные страницы команды ls
                  
cp                # используйте команду cp для копирования файлов через командную строку. 
                  # Он принимает два аргумента: первый - это местоположение файла, который нужно скопировать, второй - куда копировать.  
mv                # mv - используйте команду mv для перемещения 

locate            # команда locate используется для поиска файла

echo              # команда "echo" помогает нам перемещать некоторые данные, обычно текст, в файл. 
                  # Например, если вы хотите создать новый текстовый файл или добавить в уже созданный текстовый файл, вам просто нужно ввести «echo hello, 
                  # меня зовут hich >> new.txt». Вам не нужно разделять пробелы с помощью обратной косой черты здесь, потому что мы заключаем
                  # в две треугольные скобки, когда мы заканчиваем то, что нам нужно написать.
                  
cat               # Используйте команду cat для отображения содержимого файла.

sudo              # сделать от имени "супер пользователя"

df                # увидеть доступное дисковое пространство в каждом из разделов вашей системы

du                # узнать, как файл используется в вашей системе
